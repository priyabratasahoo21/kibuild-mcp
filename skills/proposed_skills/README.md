# Proposed KiBuild Skills

These are specialist skill templates that should not be automatically enabled yet.

Move a skill into `sidecar/skills/default_skills/` only after:

1. Workflow-based skill selection exists.
2. The skill has a clear owning workflow.
3. The prompt size and behavior have been tested.

Until then, these files are design-ready templates.

